---
name: easysdd-issue-analyze
description: Issue 工作流阶段二：根因分析。读代码找根因、评估影响面、列修复方案，产出 analysis.md。触发场景："分析这个 bug"、"找根因"、"定位问题"，且已有 report.md。前置依赖 easysdd-issue-report。
---

# easysdd-issue-analyze

本技能对应 easysdd-issue 工作流的**阶段二:根因分析**。

## 涉及的路径

本技能在正文里用自然语言术语引用路径(**问题报告**、**根因分析**、**issue 目录**),具体目录位置见主技能 `easysdd-issue` 第二节"目录安排"。

## 你的职责

读问题报告 + 读代码,找到真正的根因并评估修复风险,**不是直接开始改代码**。分析完给用户看结论和方案选项,用户确认后才进阶段三。

## 启动检查

1. **问题报告存在且已确认**。读 issue 目录下的 `report.md`,确认 5 节都有内容、用户已放行。如果 report 不完整,先回 `easysdd-issue-report`。
2. **读全上下文**:
   - 问题报告(`report.md`)全文
   - `AGENTS.md`
   - 报告里提到的相关文件(用 Glob/Grep 找,不要只凭 report 里的描述)
   - 架构中心目录 `DESIGN.md`(如果涉及跨模块问题)
  - **技巧库目录**:用 `python easysdd/tools/search-yaml.py --dir easysdd/tricks --filter status=active --query "{issue 关键词}"` 搜索 tricks 目录,看有没有相关库/框架已知坑或推荐用法。找到的话在分析开头标注("技巧库可复用,参见 tricks/xxx.md"),并说明本次是否沿用
  - **探索归档目录**:用 `python easysdd/tools/search-yaml.py --dir easysdd/explores --query "{issue 关键词}"` 搜索 explores 目录,看有没有同模块或同调用链的历史探索记录。找到的话在分析开头标注("历史探索可复用,参见 explores/xxx.md"),并说明本次是否沿用其结论
   - **知识沉淀目录**:用 `python easysdd/tools/search-yaml.py --dir easysdd/learnings --query "{issue 关键词}"` 搜索 learnings 目录,看有没有同类型的历史 pitfall 文档。找到的话在分析开头标注("此类问题历史上踩过,参见 learnings/xxx.md"),并在根因分析里说明本次是否属于同一根因

## 分析步骤

按顺序执行,每步都要**真正去读代码**,不要靠推测。

### 步骤 1:定位问题代码

根据报告里的"涉及模块/功能"和"复现步骤",用 Grep/Glob 找到相关代码:

- 搜索报告里提到的函数名、类名、文件名
- 沿调用链追溯(从用户触发的入口往下找)
- 重点关注:条件分支、边界值处理、状态更新、异步逻辑、数据流转

记录下定位到的关键位置:`{文件}:{行号} — {说明这里干什么}`。

### 步骤 2:还原失败路径

对照复现步骤,在脑子里(或实际跑一遍)把代码执行路径走一遍:

- 用户触发了什么 → 调了哪个函数 → 数据怎么流 → 哪里分叉走错了
- 画出或描述"正确路径"和"失败路径"的分叉点
- **分叉点 = 根因候选**

### 步骤 3:确认根因

- 单一根因 vs 多个根因:如果有多个根因,一一列出,并说明主次
- 根因分类(帮助后续定位修复范围):
  - **逻辑错误**:条件判断有误、边界值处理缺失
  - **状态污染**:某处副作用影响了后续流程
  - **数据格式**:输入/输出格式假设不符合实际
  - **并发/竞态**:异步顺序或共享状态问题
  - **配置/环境**:依赖了某个未稳定的配置项
  - **缺少防御**:没处理 null/undefined/空列表等边界

### 步骤 4:影响面评估

- **影响范围**:只影响报告描述的场景,还是会影响更多场景?
- **潜在受害者**:哪些其他功能/模块可能被同一根因波及?
- **数据完整性**:这个 bug 会不会导致数据损坏或状态不一致?
- **严重程度复核**:和报告里的 P0/P1/P2/P3 比较,是否需要调整?

### 步骤 5:修复方案选项

列出 2-3 种修复方向,每种说明:

- **做什么**:改哪里、怎么改(代码层面描述,不需要写代码)
- **优点**:为什么这么改好
- **缺点/风险**:改这里可能带来什么副作用
- **影响面**:改动会触碰哪些文件、会不会影响其他功能

**推荐方案**:在 2-3 种里挑一种,说明推荐理由(通常是:改动范围最小 + 根因最直接 + 副作用最少)。

## 根因分析模板

分析完后,把结论写进文件:

**路径**:根因分析(目录位置见主技能 `easysdd-issue` 第二节"目录安排")。

```markdown
# {问题简述} 根因分析

> 阶段:阶段二(根因分析)
> 状态:草稿 / 已确认
> 分析日期:YYYY-MM-DD
> 关联问题报告:{report.md 路径}

## 1. 问题定位

| 关键位置 | 说明 |
|---|---|
| `{文件}:{行号}` | {这里干什么,为什么有问题} |
| `{文件}:{行号}` | ... |

## 2. 失败路径还原

**正常路径**:
{用户做 A → 调用 B → 数据经过 C → 结果 D(符合期望)}

**失败路径**:
{用户做 A → 调用 B → 在 C 处因为 E 走了错误分支 → 结果 F(不符合期望)}

**分叉点**:`{文件}:{行号}` — {为什么这里走错了}

## 3. 根因

**根因类型**:{逻辑错误 / 状态污染 / 数据格式 / 并发竞态 / 配置环境 / 缺少防御}

**根因描述**:
{一段话说清楚为什么会发生这个问题,要能让没看过代码的人理解}

**是否有多个根因**:{是 / 否。如果是,列出并说明主次}

## 4. 影响面

- **影响范围**:{只影响报告场景 / 还会影响 X、Y、Z 场景}
- **潜在受害模块**:{列出可能被波及的其他模块或功能}
- **数据完整性风险**:{有 / 无。有的话说明风险内容}
- **严重程度复核**:{维持 P? / 调整为 P?,理由}

## 5. 修复方案

### 方案 A:{方案名}

- **做什么**:{改哪里、怎么改}
- **优点**:{...}
- **缺点/风险**:{...}
- **影响面**:{会动哪些文件,会不会影响其他功能}

### 方案 B:{方案名}

- **做什么**:{...}
- **优点**:{...}
- **缺点/风险**:{...}
- **影响面**:{...}

### 推荐方案

**推荐方案 {A / B}**,理由:{改动范围最小 / 根因最直接 / 副作用最少 + 具体说明}
```

## checkpoint:和用户对齐

写完 analysis.md 后,**不要直接开始修**。做一次对齐:

1. 把"根因"和"推荐方案"口头总结给用户(不要让用户去读整份文件)
2. 问用户:"根因判断是否准确?推荐的修复方案你是否认可,还是想选别的方向?"
3. 等用户明确确认方案后,才触发阶段三

## 退出条件

- [ ] 根因分析 5 节都填完
- [ ] 定位到了具体的代码位置(`{文件}:{行号}` 粒度)
- [ ] 失败路径还原清晰
- [ ] 影响面评估完成
- [ ] 至少列出 2 种修复方案并给出推荐
- [ ] 用户明确确认方案:"分析准确,用方案 X 修"

## 退出后

告诉用户:"根因分析已就绪,方案已确认。下一步是阶段三:修复验证。可以触发 `easysdd-issue-fix` 技能开始修复。"

不要自己顺手开始改代码——阶段之间的人工 checkpoint 是工作流的硬约束。

## 反模式(看到就停)

- ❌ 根因写"可能是某处的问题"——必须定位到具体文件:行号
- ❌ 没读代码就靠问题描述推断根因——一定要实际 Grep/Read
- ❌ 只列一种修复方案——至少要给用户两种选择
- ❌ 分析完直接开始改代码——必须等用户确认方案
- ❌ 影响面写"可能影响其他功能"——要具体说是哪些功能/模块
- ❌ 严重程度复核总是"维持原级别"——认真看影响面,确实升级的要改

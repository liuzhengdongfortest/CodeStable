---
doc_type: goal-iteration
goal: fix-skills-update-and-standalone-version
iteration: 2
status_after: complete
next_action: "PR #49 is open for repository review and merge."
blocker_signature: null
updated_at: 2026-07-14
---

# Iteration 002

## Current Understanding

#45 的仓库侧可控终态是完整 package 重装入口、可信 deletion-diff 回归与明确禁止裸 `update`；#47 的终态是 standalone 单元自带版本、缺版本零副作用 fail-closed，并由发布/checker/runtime 三方锁定一致性。

## Implementation Approach

先修复生产契约与测试，再经过 Claude Fable 高推理独立 code review、focused closure 和独立黑盒功能验收。所有 gate 通过后提交到独立分支并创建同时关闭 #45/#47 的 PR。

## Changes This Iteration

- 修复 Round 1 两个测试假阳性：离线 oracle 改为真实删除差集，CLI E2E 预装并验证第三方 probe。
- 同步完整 priority prefixes、tracked-only discovery、`force=True` 缺版本回归和 `version-unavailable` 人读说明。
- 独立 code review Round 2 verdict：passed，blocking 0 / important 0。
- 独立功能验收 verdict：pass；两条真实 `skills@1.5.17` 黑盒路径与五条 owner criteria 全部通过。
- 提交 `db36f82`，推送 `fix/issues-45-47-install-runtime`，创建 PR [#49](https://github.com/codestable/CodeStable/pull/49)。

## Verification Evidence

- 相关：`45 passed, 1 skipped`；全量：`443 passed, 1 skipped`。
- 显式真实 CLI E2E：`1 passed`，32 个 CodeStable skills 与第三方 probe 全部保留。
- Standalone 安装产物和 runtime manifest 的版本均为 `1.0.3`；缺 VERSION 的 force 两态均 `version-unavailable` 且不写 manifest。
- package checker `ok: true`；runtime sync `status: ok`；ruff、diff check、source-copy 与 Markdown ≤300 均通过。
- PR #49 为 Open，base=`main`，head=`fix/issues-45-47-install-runtime`，正文包含 `Closes #45` 与 `Closes #47`。

## Problems Encountered

首次 `gh pr create` 将正文中的反引号暴露给 shell 命令替换，导致 PR 正文损坏并意外执行一次非交互 `npx skills update` 检查；该命令对删除提示全部选择 skip，未删除 skills。随后立即用单引号保护的变量重写正文，并通过 `gh pr view` 核验远端内容正确。

## Next Attempt

无。等待 PR #49 的正常 review 与 merge；上游 `vercel-labs/skills#1298` 继续作为外部风险跟踪。

## State Update

Goal 标记 `complete`：实现、验证、独立 review、功能验收、commit、push 与 PR 均已完成，无 blocker。

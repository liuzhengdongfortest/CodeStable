---
name: cs-how-docs
description: 文档组织原则：把文档当知识空间设计，而不是内容平铺。触发：写 wiki、requirements、notes、README、文档集或整理长期知识。
---

# cs-how-docs

AI 会写单篇文档，但常常不会组织文档。它会把 `index.md` 写成目录，把一轮调查的问题平铺成 topics，把所有页面套同一个模板，看起来完成了任务，实际上没有帮后来者理解系统。

本技能教的是文档组织，不是文风。它回答：这些知识应该怎样分层、入口在哪里、读者按什么路径读、当前真相和历史证据放哪。

## 背景

文档不是内容容器，而是后来者理解系统的路径设计。一个好的文档系统不追求把材料都塞进去，而追求让读者知道先看什么、带着什么问题往下走、在哪里找细节、在哪里看当前真相、在哪里看历史和证据。

CodeStable 里的 `requirements/`、`wiki/`、`notes/`、README 和技能文档都会越写越多。如果没有组织判断，它们会变成文件垃圾场：每篇都能看，合起来不能用。

## 核心概念

**读者任务。** 先判断谁会读：新人理解系统、实现者改代码、维护者排障、用户确认需求，还是未来 AI 复用经验。读者不同，入口和层级就不同。

**主文档。** 主文档不是目录。它要统帅这个知识域：解释它是什么、为什么存在、核心概念、全局地图、重要入口和下一步阅读路径。索引只是主文档的一部分。

**层级。** 文档之间要有抽象关系：总览、领域、机制、流程、证据、历史、候选。不能把所有 topic 平铺成同级页面。

**阅读路径。** 链接不是为了显得有关联，而是告诉读者下一步去哪：关心 X 读哪篇；要修改 Y 先看什么；只想确认需求看哪里。

**真相分层。** 当前真相、候选理解、历史过程和操作经验不能混在一起。`requirements/` 放当前需求真相，`wiki/` 放系统理解和候选，`issues/` 放变更历史，`notes/` 放可复用经验。

## 判断框架

写或整理文档前，先回答五个问题：

1. 这批内容服务哪个读者任务？
2. 哪一篇是主文档，它应该统帅什么？
3. 哪些内容是总览、领域、机制、流程、证据、历史或候选？
4. 读者最自然的三条阅读路径是什么？
5. 哪些内容应该合并、拆分、降级为证据，或迁移到别的实体？

组织文档时，按问题和结构分，不按产出批次分。一次调查问了三个问题，不等于应该生成三个 topic；要看它们分别属于数据流、控制流、模块责任、概念词汇、用户路径、外部依赖还是边界行为。

主文档要用说明文建立地图，再给入口。避免只有表格。表格可以作为索引，但不能替代对系统结构、读者路径和重要性排序的解释。

每篇子文档只承担一个清楚职责。它可以按自己的材料长成不同形状：数据流文档讲流向和变形，概念文档讲术语和不变量，流程文档讲步骤和失败点，需求文档讲当前规则和取舍。不要为了整齐强迫所有文档使用同一组标题。

链接要带意图。不要只堆文件名；要说明为什么读它、读完能解决什么问题、什么时候不需要读。

## 反例

- `index.md` 只有一张 topic 表，没有讲这个知识域的目标、地图和阅读路径。
- `topics/` 以轮次、问题编号或调查批次平铺，而不是按系统结构组织。
- 每篇 topic 都套同一个模板，导致概念、流程、数据流和证据文档长得一样。
- 把尚未确认的 wiki 候选写进 requirements，或把关闭 issue 的历史过程塞进当前真相。
- 链接只是互相引用，没有告诉读者“为什么去那里”。

## 应用场景

写或重组 wiki、requirements、notes、README、技能文档、项目手册和多文件知识库时使用。尤其适合：文档越来越多但不好找、index 变成目录、topic 平铺、AI 生成的文档看似完整但读者仍不知道该从哪开始。

不适用：润色单篇文章语气、生成对外营销文案、制作 API 逐项参考。那些可以有自己的写作或文档技能。

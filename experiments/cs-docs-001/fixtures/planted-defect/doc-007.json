{
  "id": "doc-007",
  "answerType": "findings-recall",
  "answer": [
    "方法参数与返回",
    "抛出的异常",
    "线程安全性与示例"
  ],
  "task": {
    "kind": "docs",
    "spec": "给 SDK 的核心方法写 reference",
    "audience": "开发者",
    "diff": "class Client:\n  def __init__(self, api_key: str, timeout: float = 10.0)\n  def fetch(self, id: str) -> Record   # raises NotFound, AuthError\n  def create(self, data: dict) -> Record  # raises ValidationError\n  # Client is NOT thread-safe; one per thread."
  }
}

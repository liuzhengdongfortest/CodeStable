interface:
  display_name: "CS MakeTools"
  short_description: "Capture guided workflows as reusable CodeStable knowledge"
  default_prompt: "Use $cs-maketools to learn an unknown guided workflow, write required notes, add a facts reference, and create tools only for stable repeatable parts."

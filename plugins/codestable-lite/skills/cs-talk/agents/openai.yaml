interface:
  display_name: "CS Talk"
  short_description: "先讨论澄清模糊想法，确认后整理 talk"
  default_prompt: "Use $cs-talk to investigate existing repo context, ask only for missing information, clarify the real need through discussion, and write to .cs/talks/YYYY/MM/DD/{短语}.md only after the user confirms the discussion should be saved."

interface:
  display_name: "CS Plan"
  short_description: "把 talks 转成带模板的 epics 或 open issues"
  default_prompt: "Use $cs-plan to turn a clarified discussion into dated epics or open issues with complete issue frontmatter."
